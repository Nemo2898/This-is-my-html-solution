Frontend for E‑Tendering System

How to run (static):
- Serve this directory with any static server (or via backend static mapping).
- The app calls the backend at `/api/tenders`.

Pages:
- `tenders.html`: Public guest listing and tender details.
- `staff.html`: Staff inspection and judging (update) form.


